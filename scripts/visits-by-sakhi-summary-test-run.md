# Test Run — Visit Due / Visit 3 Days to Expire / Missed Visit call-sheet rollup

Repo: ARMMAN-Organization/arogyasakhi-service
Branch: feature/supervisor-approval-flow
Endpoint: `GET /visits/by-sakhi/:sakhiId/summary` (new — proxies visit-form-service's `/visits/visit-summary` from supervisor-operations-service)

Automated coverage: no dedicated `.spec.ts` was added for this route (it's a thin pass-through with no new business logic — see `visitSummary.client.ts`'s doc comment for why no roster check is duplicated here). `npm run lint && npm run affected:test` covers the rest of the diff (212 existing supervisor-operations-service tests still pass unmodified except constructor signature updates). Use this file to track the manual/API-level verification below before sign-off.

| #   | Area                              | Type   | Preconditions                                | Steps                                                                                        | Expected Result                                                                                                                  | Status                                                                    |
| --- | --------------------------------- | ------ | -------------------------------------------- | -------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------- |
| 1   | Happy path                        | API    | Supervisor token, sakhiId in caller's roster | `GET /api/v1/visits/by-sakhi/:sakhiId/summary`                                               | 200, `data: {total, byStatus, endingSoonVisitsCount}`                                                                            | ✅ Verified (curl through gateway, port 3000)                             |
| 2   | Date range filter                 | API    | Same as #1                                   | `GET .../summary?fromDate=2026-08-01&toDate=2026-08-21`                                      | 200, filtered range passed through to visit-form-service                                                                         | ✅ Verified                                                               |
| 3   | MANAGER/ADMIN unrestricted        | API    | Manager/Admin token                          | `GET .../summary` for any sakhiId                                                            | 200, no roster restriction                                                                                                       | ✅ Verified (MANAGER via smoke script)                                    |
| 4   | Invalid sakhiId                   | API    | —                                            | `GET .../not-a-uuid/summary`                                                                 | 400 `VALIDATION_ERROR`                                                                                                           | ✅ Verified                                                               |
| 5   | Invalid date format               | API    | —                                            | `?fromDate=08-01-2026`                                                                       | 400 `VALIDATION_ERROR`                                                                                                           | ✅ Verified                                                               |
| 6   | Unknown query param               | API    | —                                            | `?foo=bar`                                                                                   | 400 (`.strict()` schema)                                                                                                         | ✅ Verified                                                               |
| 7   | Sakhi outside roster              | API    | Supervisor token, sakhiId NOT in roster      | `GET .../summary`                                                                            | 403, passed through from visit-form-service as-is (not masked as 502)                                                            | ✅ Verified                                                               |
| 8   | SAKHI role forbidden              | API    | Sakhi token                                  | `GET .../summary`                                                                            | 403 — this route is supervisor-facing only, unlike the underlying visit-form-service endpoint which a Sakhi may call for herself | ✅ Verified                                                               |
| 9   | No Authorization header           | API    | —                                            | `GET .../summary`                                                                            | 401                                                                                                                              | ✅ Verified                                                               |
| 10  | No gateway identity headers       | API    | —                                            | `GET .../summary`                                                                            | 401                                                                                                                              | ✅ Verified                                                               |
| 11  | visit-form-service unreachable    | Manual | Stop visit-form-service                      | `GET .../summary`                                                                            | 502 `BAD_GATEWAY` from `visitSummary.client.ts`, not a hang or 500                                                               | ☐ Not run — requires stopping a shared dev dependency                     |
| 12  | visit-form-service 404s the sakhi | Manual | sakhiId doesn't exist at all                 | `GET .../summary`                                                                            | 404 passed through as-is                                                                                                         | ☐ Not run — needs a genuinely nonexistent sakhiId, not just out-of-roster |
| 13  | Gateway route ordering regression | API    | —                                            | `GET /api/v1/visits/visit-summary?sakhiId=...` (existing generic route)                      | Still 200, unaffected by the new `/visits/by-sakhi` prefix registered ahead of it                                                | ✅ Verified — no regression                                               |
| 14  | Full regression                   | CI     | —                                            | `npx nx run supervisor-operations-service:test` and `:build`, `npx nx run api-gateway:build` | All green                                                                                                                        | ✅ 212/212 tests pass, both builds succeed                                |

## Notes / gaps to flag in review

- Rows 11–12 weren't run live (would require tearing down a shared dev dependency others may be using) — worth a manual check before merge if this is going to prod-like staging.
- No dedicated `.spec.ts` for `VisitSummaryClient` or the new route/controller — flag to the reviewer whether a unit test (mocking `fetch`) should be added given the CLAUDE.md ≥70% coverage target, even though the logic is thin.
