# @armman/core

Framework-agnostic helpers (Result type, date/string utilities, validators) shared
across backend services. No NestJS or framework dependencies — safe to use anywhere,
including in an extracted ("forklifted") service.
