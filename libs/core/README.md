# @armman/core

Framework-agnostic helpers (Result type, date/string utilities, validators) shared
across backend services. No Express or other framework dependencies — safe to use
anywhere, including in an extracted ("forklifted") service.
