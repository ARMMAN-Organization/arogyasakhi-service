NODE_ENV=development
PORT=3001
LOG_LEVEL=debug
DATABASE_URL=postgresql://postgres.PROJECT_REF:REPLACE_ME@aws-0-ap-northeast-1.pooler.supabase.com:5432/postgres?schema=beneficiary
DIRECT_URL=postgresql://postgres.PROJECT_REF:REPLACE_ME@aws-0-ap-northeast-1.pooler.supabase.com:5432/postgres?schema=beneficiary
CORS_ORIGINS=http://localhost:5173
# Base64-encoded 32-byte keys (e.g. `openssl rand -base64 32`). Distinct keys —
# never reuse one for both. Real values live only in the gitignored .env.
PII_ENCRYPTION_KEY=REPLACE_ME
PII_SEARCH_HASH_KEY=REPLACE_ME
